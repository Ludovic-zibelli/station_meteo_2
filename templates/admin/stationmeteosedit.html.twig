{% extends 'base.html.twig' %}

{% block title %}Tableau de bord du site{% endblock %}

{% block body %}

<?php header('Access-Control-Allow-Origin: *'); ?>
<div class="container-fluid">
    <h1 class="text-center">Tableau de bord du site</h1>
    <a href="{{ path ('gestionstation')}}" class="">Retour</a>

</nav>

<div class="card">
    <div class="card-header text-center">
      <h3>Modifier la station météo</h3>
    </div>
    <div class="card-body">
    {{ form_start(form) }}
    <div class="row">
        <div class="col">
            <h6 class="card-subtitle mb-2 text-muted">Ville :</h6>
            {{ form_widget(form.ville) }}
        </div>
        <div class="col">
            <h6 class="card-subtitle mb-2 text-muted">Code postal :</h6>
            {{ form_widget(form.codepostal) }}
        </div>   
    </div>
</br>
    <div class="row">
        <div class="col">
            <h5 class="text-center">Localisation GPS</h5>
        </div>
    </div>
    <div class="row">
        <div class="col">
            <h6 class="card-subtitle mb-2 text-muted">Latitude :</h6>
            {{ form_widget(form.gps_latitude) }}
        </div>
        <div class="col">
            <h6 class="card-subtitle mb-2 text-muted">Longitude :</h6>
            {{ form_widget(form.gps_longitude) }}
        </div>
    </div>
    <div class="row">
        <div class="col">
            <h6 class="card-subtitle mb-2 text-muted">User associer :</h6>
            {{ form_widget(form.user) }}
        </div>
        <div class="col">
            <h6 class="card-subtitle mb-2 text-muted">DIY :</h6>
            {{ form_widget(form.diy) }}
        </div>
    </div>
    <div class="row">
        <div class="col">
            <h6 class="card-subtitle mb-2 text-muted">Description :</h6>
            {{ form_widget(form.description) }}
        </div>
    </div>
    <div class="row">
        <div class="col">
            <h6 class="card-subtitle mb-2 text-muted">Photo :</h6>
            {{ form_widget(form.filePhoto) }}
            <img src="{{ vich_uploader_asset(stationmeteos, 'filePhoto') }}"  width=100% height=auto alt="Image Station">
        </div>
    </div>
</br>
    <div class="row">
        <div class="col">
            <button class="btn btn-primary btn-block" type="submit">Modifier la nouvelle station</button>
        </div>
    </div>

    {{ form_end(form)}}
    </div>
</div>


</div>
{% endblock %}